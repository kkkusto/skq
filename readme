let
    PreviousStep = <LastStepName>,
    RemoveEmptyColumns = Table.SelectColumns(PreviousStep, List.Select(Table.ColumnNames(PreviousStep), (col) => List.NonNullCount(Table.Column(PreviousStep, col)) > 0))
in
    RemoveEmptyColumns
