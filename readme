SELECT
    FORMAT(row_insert_time, 'yyyy-MM') AS MonthYear,
    COUNT(*) AS RowCount
FROM your_table_name
WHERE row_insert_time >= DATEADD(YEAR, -5, GETDATE())
GROUP BY FORMAT(row_insert_time, 'yyyy-MM')
ORDER BY MonthYear;
